# Modules

Esta pasta fica reservada para a service layer por domínio:

- `auth`
- `companies`
- `branches`
- `products`
- `stock`
- `orders`
- `deliveries`
- `reports`

Na próxima etapa, a lógica hoje concentrada em `src/lib/domain.js` deve ser movida para módulos tipados e orientados a domínio.
